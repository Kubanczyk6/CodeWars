package kata7.sortArrays;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.IntStream;

public class SortArray {
    public static void main(String[] args) {
        System.out.println(Arrays.toString(sortArray(new int[]{1, 5, 6})));
    }

    public static int[] sortArray(int[] names) {
        Arrays.sort(names);
        return names;
    }

    public static float[] sortArray(float[] names) {
        Arrays.sort(names);
        return names;
    }

    public static double[] sortArray(double[] names) {
        Arrays.sort(names);
        return names;
    }

    public static long[] sortArray(long[] names) {
        Arrays.sort(names);
        return names;
    }

    public static String[] sortArray(String[] names) {
        Arrays.sort(names);
        return names;
    }

    public static Integer[] sortArray(Integer[] names) {
        Arrays.sort(names);
        return names;
    }


}
